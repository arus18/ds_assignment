{
  "name": "itemservice",
  "version": "1.0.0",
  "description": "Service to handle all functions related to item",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "build":"docker build -t stargazerdocker/herbal-docker-items-service:latest . && docker push stargazerdocker/herbal-docker-items-service:latest",
    "deploy": "kubectl replace --force -f ./k8s/items-service.yaml"
  },
  "author": "Arus",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^7.0.3"
  }
}
